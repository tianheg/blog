{
	"name": "blog",
	"private": true,
	"scripts": {
		"clean": "rimraf public",
		"pagefind": "pagefind --source public",
		"build": "hugo --buildFuture",
		"start": "hugo serve --disableFastRender --buildFuture",
		"all": "npm-run-all clean build pagefind"
	},
	"devDependencies": {
		"npm-run-all": "^4.1.5",
		"rimraf": "^4.1.2"
	},
	"dependencies": {
		"pagefind": "^0.10.7"
	}
}
