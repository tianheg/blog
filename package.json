{
	"name": "blog",
	"private": true,
	"scripts": {
		"pagefind": "cd scripts/ && tar xvf pagefind_extended-v0.11.0-x86_64-unknown-linux-musl.tar.gz && pagefind_extended --source public --bundle-dir ../static/_pagefind && cd ..",
		"build": "hugo --gc --buildFuture",
		"start": "hugo serve --buildFuture --disableFastRender --renderStaticToDisk",
		"all": "npm-run-all build pagefind build"
	},
	"devDependencies": {
		"npm-run-all": "^4.1.5"
	},
	"dependencies": {
		"sass-embedded-linux-x64": "^1.57.1"
	}
}
