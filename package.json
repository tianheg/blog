{
	"name": "blog",
	"private": true,
	"scripts": {
		"clean": "rimraf public",
		"pagefind": "./scripts/pagefind_extended --source public --bundle-dir ../static/_pagefind",
		"build": "hugo --buildFuture",
		"start": "hugo serve --disableFastRender --buildFuture",
		"all": "npm-run-all pagefind build"
	},
	"devDependencies": {
		"npm-run-all": "^4.1.5",
		"rimraf": "^4.1.2"
	}
}
