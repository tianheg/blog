{
	"name": "blog",
	"private": true,
	"scripts": {
		"music": "node scripts/music.js",
		"pagefind": "pagefind_extended --source public --bundle-dir ../static/_pagefind",
		"build": "hugo --gc --buildFuture",
		"start": "hugo serve --buildFuture --disableFastRender --renderStaticToDisk",
		"all": "npm-run-all music build pagefind build"
	},
	"devDependencies": {
		"npm-run-all": "^4.1.5"
	},
	"dependencies": {
		"ejs": "^3.1.8",
		"sass-embedded-linux-x64": "^1.57.1"
	}
}
