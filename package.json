{
  "name": "blog",
  "private": true,
  "scripts": {
    "pagefind": "pagefind_extended --site public --write-playground",
    "build": "hugo --gc --minify --buildFuture",
    "dev": "hugo serve --buildFuture",
    "all": "run-s build pagefind",
    "lint": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.6",
    "@tailwindcss/cli": "^4.1.14",
    "@tailwindcss/typography": "^0.5.19",
    "daisyui": "^5.2.3",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^4.1.14"
  }
}
