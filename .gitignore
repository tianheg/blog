public
resources
.hugo_build.lock
*.org#
*~
node_modules/
static/_pagefind

# Build-time fetched data (regenerated by scripts/fetch-watch-data.sh)
data/watch.json

.vscode
.venv-fonttools

# Obsidian vault 产物（content/ 同时是 Obsidian vault，这些不该进仓库、也不该上线）
.obsidian/
*.canvas
.trash/

# CI build artifacts (scripts/build.sh extracts Hugo release in project root)
hugo
LICENSE
