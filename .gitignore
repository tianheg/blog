public
resources
.hugo_build.lock
*.org#
*.md#
*~
node_modules/
static/_pagefind

# Build-time fetched data (regenerated by scripts/fetch-watch-data.sh)
data/watch.json

.vscode
.venv-fonttools

# 编辑器产物保险（Obsidian 已不再使用；忽略规则保留，防手滑提交/上线）
.obsidian/
*.canvas
.trash/

# CI build artifacts (scripts/build.sh extracts Hugo release in project root)
hugo
LICENSE

# Agent 会话产物（计划文件等，不进版本控制）
.hermes/
